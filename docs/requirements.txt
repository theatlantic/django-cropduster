# Build dependencies for the Sphinx documentation, installed by
# .readthedocs.yaml. conf.py reads the version out of cropduster/__init__.py
# with a regex rather than importing it, so cropduster itself is not needed.
sphinx==8.1.3
sphinx-rtd-theme==3.1.0
